# General
set show_hidden true

# Enable this if key combinations with the Alt Key don't work for you.
# (Especially on xterm)
# set xterm_alt_key true

# Use fuzzy tab completion with the "cd" command. For example,
# ":cd /u/lo/b<tab>" expands to ":cd /usr/local/bin".
set cd_tab_fuzzy true

# Save tabs on exit
# set save_tabs_on_exit true

# Flush the input after each key hit?  (Noticeable when ranger lags)
# set flushinput true

# Wrap long lines in plain text previews?
# set wrap_plaintext_previews false

# Draw borders around columns? (separators, outline, both, or none)
# Separators are vertical lines between columns.
# Outline draws a box around all the columns.
# Both combines the two.
set draw_borders both

# Preview images
set preview_images true
# set preview_images_method kitty
set preview_images_method iterm2

# Display the directory name in tabs?
set dirname_in_tabs true

# Show hostname in titlebar?
# set hostname_in_titlebar false

# Ask for a confirmation when running the "delete" command?
# Valid values are "always", "never", "multiple" (default)
# With "multiple", ranger will ask only if you delete multiple files at once.
set confirm_on_delete never

# Automatically count files in the directory, even before entering them?
set automatically_count_files false

# Abbreviate $HOME with ~ in the titlebar (first line) of ranger?
set tilde_in_titlebar true

# State of the four backends git, hg, bzr, svn. The possible states are
# disabled, local (only show local info), enabled (show local and remote
# information).
set vcs_backend_git disabled

# Key mapping
map <Esc >q
map <C-t >tab_new

map z console z%space
map <DELETE >console delete

map <HOME >move up=7
map <END >move down=7

map K move up=7
map J move down=7

map <PAGEUP >scroll_preview -7
map <PAGEDOWN >scroll_preview 7

map <A-1 >cd ~
map <A-2 >cd ~/pnphuong29
map <A-3 >cd ~/pnphuong29/projects/github.com/pnphuong29
map <A-4 >cd ~/scripto-common/vendors
