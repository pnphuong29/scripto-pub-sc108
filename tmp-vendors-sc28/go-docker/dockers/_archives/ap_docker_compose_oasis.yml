version: '3.9'

services:
    oasis:
        container_name: sc28-oasis
        image: machengim/oasis
        restart: always
        tty: true
        ports:
            # - '${AP_PORT_JOPLIN_WEBDAV}:${AP_PORT_JOPLIN_WEBDAV}'
            - 9966:8000
        # volumes:
        # - ./filebrowser/volume/database/filebrowser.db:/database/filebrowser.db
        networks:
            - sc28
