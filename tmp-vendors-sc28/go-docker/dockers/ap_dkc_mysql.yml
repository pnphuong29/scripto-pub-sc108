# https://hub.docker.com/_/mysql
version: ${AP_DKCF_VER}

services:
    mysql:
        container_name: mysql
        image: mysql:8.0
        # build:
        #     context: ./mysql
        #     args:
        #         VERSION: 8.0
        #         AP_DK_CON_MYSQL: mysql
        #     dockerfile: ./dockerfiles/mysql.Dockerfile
        environment:
            MYSQL_ROOT_PASSWORD_FILE: /run/secrets/mysql_root_pass
            # MYSQL_USER:  # Create a new user
            # MYSQL_PASSWORD: /run/secrets/mysql_pass # Password for the newly created user
            # MYSQL_DATABASE:
        restart: always
        tty: true
        ports:
            - ${AP_PORT_MYSQL_PUBLIC}:3306
        volumes:
            - ${AP_SERVER_MYSQL_DB_DIR}:/var/lib/mysql
            - ${AP_SERVER_MYSQL_BK_DIR}/db:/backups
            # - ./entrypoints/ap_entrypoint_mysql.sh:/ap_entrypoint.sh:ro
        # entrypoint: /ap_entrypoint.sh
        # NOTE: use of "mysql_native_password" is not recommended: https://dev.mysql.com/doc/refman/8.0/en/upgrading-from-previous-series.html#upgrade-caching-sha2-password
        # command: --default-authentication-plugin=mysql_native_password
        command: --default-authentication-plugin=caching_sha2_password
        networks:
            - sc28
        secrets:
            - mysql_root_pass
